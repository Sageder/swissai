// Firebase components removed - now using mock data from JSON files
// This file is kept for backward compatibility but exports are no longer functional

console.warn("Firebase components have been removed. The application now uses mock data from JSON files.");

// Mock exports to prevent import errors
export const db = null;
export const auth = null;
export default null;